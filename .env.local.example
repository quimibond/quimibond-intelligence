# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://tozqezmivpblmcubmnpi.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service key (server-side only, used by /api/chat)
SUPABASE_SERVICE_KEY=your-service-key

# Claude API (server-side only, used by /api/chat)
ANTHROPIC_API_KEY=your-anthropic-key

# Auth password (optional — if not set, dashboard is open)
AUTH_PASSWORD=
